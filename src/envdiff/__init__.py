"""envdiff — environment variable drift detector.

Compare two sources of environment variables and report ADDED / REMOVED /
CHANGED / UNCHANGED keys. Fully local, no API keys.
"""

__version__ = "0.1.0"
